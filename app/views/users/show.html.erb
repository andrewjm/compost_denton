<% provide( :title, 'Profile' ) %>
<div class="row">
  <div class="col-md-12">
    <h1><%= @user.first_name %><!-- going to change this to org_name --></h1>
    <h2>Total Diversion<span class="pull-right"><%= @user_weight %> lbs</span></h2>
    <h2>Monthly Average<span class="pull-right">xx lbs</span></h2>
    <h2>Weekly Average<span class="pull-right">xx lbs</span></h2>
    <%= link_to "Log weight", user_members_path(@user), class: "btn btn-primary btn-lg" %>
    <%= link_to "Add member", new_user_member_path(@user), class: "btn btn-default btn-lg"  %>
  </div>
</div>
